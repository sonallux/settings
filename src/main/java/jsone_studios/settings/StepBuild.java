package jsone_studios.settings;

public interface StepBuild
{
    /**
     * Builds the Settings object with the Elements specified by this {@link SettingsBuilder}
     *
     * @return the new Settings object
     */
    Settings build();
}
